function tab(){
    var te=prompt("PRESIONE 1 PARA VER LA TABLA DE CALAVERITAS, PRESIONE 2 PARA VER LA TABLA DE DISFRACES: ");

    if (te==1) {
        window.location="http://localhost/tabla/";}
        else {
            window.location="http://localhost/tabla2/";       
    }
}